class Carrier {
    constructor(id, name, email, confirmation_number, business_id ) {
        this.id = id,
        this.name = name,
        this.email = email,
        this.confirmation_number = confirmation_number,
        this.business_id = business_id
    }
}